services:
  db:
    image: postgres:16
    container_name: backend_postgres
    command: ["postgres", "-c", "statement_timeout=60000"]
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5433:5432"  # Consider removing or restricting in production
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - webline-backend-network
      - webline-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7.0-alpine
    container_name: backend_redis
    ports:
      - "6379:6379"  # Remove in production to restrict access
    volumes:
      - redis-data:/data
    networks:
      - webline-backend-network
      - webline-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD}"]
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    restart: unless-stopped

  backend:
    image: ${DOCKER_HUB_USERNAME}/webline-backend:latest
    container_name: backend
    env_file:
      - .env
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_BUCKET_NAME=${AWS_BUCKET_NAME}
      - BUSINESS_SHORTCODE=${BUSINESS_SHORTCODE}
      - PASSKEY=${PASSKEY}
      - CALLBACK_URL=${CALLBACK_URL}
      - CONSUMER_KEY=${CONSUMER_KEY}
      - CONSUMER_SECRET=${CONSUMER_SECRET}
      - ACCOUNT_REFERENCE=${ACCOUNT_REFERENCE}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USERNAME=${SMTP_USERNAME}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
      - FROM_EMAIL=${FROM_EMAIL}
      - FROM_NAME=${FROM_NAME}
      - TO_EMAIL=${TO_EMAIL}
      - FRONTEND_URL=${FRONTEND_URL}
      - BACKEND_URL=${BACKEND_URL}
      - AWS_REGION=${AWS_REGION}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    ports:
      - "8080:8080"
    volumes:
      - ./backup.sh:/app/backup.sh:ro
      - ./backups:/app/backups
      - ./sql/schema:/app/sql/schema:ro
      - ./wait-for-it.sh:/app/wait-for-it.sh:ro
      - ./entrypoint.sh:/app/entrypoint.sh:ro
      - ./crontab-appuser:/etc/crontabs/appuser:ro
      - ./backend.env:/app/.env:ro  # Mount the .env file
    entrypoint: ["/app/entrypoint.sh"] # Use entrypoint.sh
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - webline-backend-network
      - webline-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: "512M"
        reservations:
          cpus: "0.2"
          memory: "256M"

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local
  backups:
    driver: local

networks:
  webline-backend-network:
  webline-network:
